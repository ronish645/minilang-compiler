fn add(a, b) {
    return a + b;
}

let x = 10;
let y = 20;
let total = add(x, y);
print(total);

if (total > 20) {
    print("greater than twenty");
} else {
    print("small value");
}

for (let i = 0; i < 3; i++) {
    print(i);
}
